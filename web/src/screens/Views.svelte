<!-- Stub: the real Views screen (spec §3.3 cards) lands in the next task. -->
<main>
  <!-- @wc-context: compiled shelf -->
  <h2>Views</h2>
  <p>not yet built — this screen lands in the next task</p>
</main>

<style>
  main {
    flex: 1;
    overflow-y: auto;
    padding: 24px 28px 30px;
  }

  h2 {
    margin: 0 0 10px;
    font: 800 24px var(--font-display);
    letter-spacing: -0.03em;
  }

  p {
    font: 400 12.5px var(--font-data);
    color: var(--faint);
  }
</style>
