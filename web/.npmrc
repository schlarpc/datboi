# node_modules comes from nix (importNpmLock; the devshell symlinks it from
# the store), so `npm install` should only ever edit package-lock.json.
# Outside nix: `npm install --package-lock-only=false` for a local install.
package-lock-only=true
update-notifier=false
fund=false
